# targets management

# cm_add_executable(<name> [ WIN32 ] [ sources... ])

# cm_add_library(<name> [ STATIC | SHARED | MODULE ] [ sources... ])

# cm_add_plugin(<name> NAME name [ VERSION version ] [ QML_SOURCES ... ] [ CPP_SOURCES ... ])

# cm_add_plugin(<name> NAME name [ VERSION version ] [ cpp_sources... ])

# cm_add_translation(<name> [ TS_SOURCES ... ])

# cm_add_apk(<name> core_target)*

# cm_add_aab(<name> core_target)*

# cm_finalize_target(target)


# project management

# cm_finalize_project()


# utilities

# target_headers(target [ PUBLIC ... ] [ PRIVATE ... ])

# cm_generate_nested_headers(dest_dir headers...)

# copy_install(...)


# Properties on targets

# Executables
# - ANDROID_BUILD_DIR
# - ANDROID_PACKAGE_DIR